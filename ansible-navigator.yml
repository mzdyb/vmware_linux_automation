---
ansible-navigator:
  ansible:  
    config:
      path: ansible.cfg
  execution-environment:
    image: hub.rh.lab/ee-vmware:1.1
    pull:
      policy: missing
  playbook-artifact:
    enable: false
